spring:
  config:
    import: optional:file:.env[.properties]

  application:
    name: go-cagong

  # 파일 업로드 크기 설정
  servlet:
    multipart:
      max-file-size: 10MB      # 파일 하나당 최대 크기
      max-request-size: 50MB   # 전체 요청 최대 크기 (여러 파일 합계)

  # 재일 db
  datasource:
    url: jdbc:mariadb://localhost:3307/go_cagong
    username: root
    password: jaeile1
    driver-class-name: org.mariadb.jdbc.Driver

  # 재웅 db
#  datasource:
#    url: jdbc:mariadb://localhost:3306/go_cagong
#    username: root
#    password: Wodnd0613!~
#    driver-class-name: org.mariadb.jdbc.Driver

  # 배포 db
#  datasource:
#    url: jdbc:mariadb://localhost:3306/go_cagong
#    username: go_cagong
#    password: gocagong2025!#
#    driver-class-name: org.mariadb.jdbc.Driver


  jpa:
    hibernate:
      ddl-auto: update     # 개발 중 update, 운영 시 validate 권장
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MariaDBDialect
    show-sql: true

  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: ${KAKAO_CLIENT_ID}
            client-secret: ${KAKAO_CLIENT_SECRET}
            client-authentication-method: client_secret_post
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            scope:
              - profile_nickname
              - profile_image
              - account_email
            client-name: Kakao

          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            authorization-grant-type: authorization_code
            scope:
              - profile
              - email
            client-name: Google

          naver:
            client-id: ${NAVER_CLIENT_ID}
            client-secret: ${NAVER_CLIENT_SECRET}
            client-authentication-method: client_secret_post
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            scope:
              - name
              - email
              - nickname
              - profile_image
            client-name: Naver


        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

          google:
            authorization-uri: https://accounts.google.com/o/oauth2/v2/auth
            token-uri: https://oauth2.googleapis.com/token
            user-info-uri: https://www.googleapis.com/oauth2/v3/userinfo
            user-name-attribute: sub

          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-name-attribute: response



jwt:
  secret: ${JWT_SECRET_KEY}
  access-token-expiration: 1800000      # 30분
  refresh-token-expiration: 604800000   # 7일

cors:
  allowed-origins: "http://localhost:3000"
  allowed-methods: "GET,POST,PUT,DELETE,OPTIONS"
  allowed-headers: "*"
  allow-credentials: true

# AWS S3
cloud:
  aws:
    credentials:
      access-key: ${AWS_ACCESS_KEY}
      secret-key: ${AWS_SECRET_KEY}
    s3:
      bucket: go-cagong-bucket
    region:
      static: eu-north-1  # 스톡홀름 리전으로 변경!
    stack:
      auto: false